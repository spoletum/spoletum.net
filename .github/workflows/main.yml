on: [push]

jobs:
  
  terraform:

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: checkout@v3
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
      - name: Terraform Init
        run: terraform init