# This inventory represents the k0s cluster in the dev environment
all:
  children:
    k0s_controller:
      vars:
        ansible_user: ansible
        ansible_ssh_private_key_file: /home/github/.ssh/elysium_dev
      hosts:
        spoletum.net: